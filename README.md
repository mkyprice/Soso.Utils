# Soso.Utils

## Installation
Install via UPM by going to 'Package Manager' -> '+' -> 'Add package from git URL...' in the Unity Editor.

Note target the 'package' folder by using '?path=package'

Example: 'https://github.com/mkyprice/Soso.Utils.git?path=package'

